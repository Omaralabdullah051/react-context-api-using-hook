//Context API is a very useful,powerful and smarter pattern to manage state. The useContext hook makes our life so much easy. By using these we don't need to wrap the child component by Context.Consumer and don't need to follow the render props pattern in a functional component.

//useContext hook is much more easier than wrap the child component by Context.Consumer and render props pattern.